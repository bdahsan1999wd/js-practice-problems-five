function make_avg(array){
    // console.log('inside the array', array);
    for(let i = 0; i < array.length; i++){
        const index = i;
        // console.log(index);
        const element = array[index];
        // console.log(element);
    }

}
const numberArray = [60, 58, 59];
const avgOfAnArray = make_avg(numberArray);
console.log(avgOfAnArray);